// TODO(Phase 2): delete once real design tokens exist — this only exists to
// give the @/ alias smoke test (src/__tests__/path-alias.smoke.test.ts)
// something real to import.
export const PLACEHOLDER_TOKEN = "unitask";
